# Отчёты прогона тестов

Сюда пишет `./gradlew :service:testReport -Pchapter=N` — файл `tests-0N.txt`.
Отчёт коммитится в pull request главы: его читают ревьюер и ИИ-проверка.
CI перегенерирует отчёт и сверяет с закоммиченным — править файл руками
бесполезно.

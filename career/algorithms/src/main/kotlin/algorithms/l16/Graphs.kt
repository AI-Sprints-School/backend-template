package algorithms.l16

/**
 * Урок 16. Обход графа и live-coding без ИИ.
 *
 * Граф без весов задан списком смежности: ключ — вершина, значение — список
 * соседей (рёбра ненаправленные, соседство уже дублировано в обе стороны).
 * Над функцией допишите вторую строку KDoc вида «Сложность: время O(?),
 * память O(?)» — так, как код будет работать после вашей реализации.
 */

/**
 * Длина кратчайшего пути от start до end числом рёбер. Если start == end,
 * возвращает 0. Если пути нет, возвращает -1.
 */
fun shortestPathLength(graph: Map<Int, List<Int>>, start: Int, end: Int): Int {
    TODO("Урок 16: BFS — кратчайший путь без весов даёт обход в ширину, не в глубину")
}

package algorithms.l15

/**
 * Урок 15. Сортировка, интервалы и бинарный поиск.
 *
 * Три задачи. Над каждой функцией допишите вторую строку KDoc вида
 * «Сложность: время O(?), память O(?)» — так, как код будет работать после
 * вашей реализации.
 */

/**
 * Сжимает массив чисел в строку диапазонов: числа сортируются, идущие
 * подряд без пропуска объединяются через дефис, разные диапазоны —
 * через запятую без пробела.
 * Пример: [1, 4, 5, 2, 3, 9, 8, 11, 0] → "0-5,8-9,11".
 */
fun compressToRanges(values: List<Int>): String {
    TODO("Урок 15: отсортируйте и одним проходом стройте диапазоны подряд идущих чисел")
}

/**
 * Сливает пересекающиеся и смежные интервалы в списке.
 * Интервал (a, b) означает отрезок [a, b] включительно; интервалы
 * [1, 3] и [3, 5] считаются пересекающимися (смежными по границе).
 * Пример: [(1, 3), (2, 6), (8, 10), (15, 18)] → [(1, 6), (8, 10), (15, 18)].
 */
fun mergeIntervals(intervals: List<Pair<Int, Int>>): List<Pair<Int, Int>> {
    TODO("Урок 15: отсортируйте по началу и идите слева направо, объединяя пересекающиеся")
}

/**
 * Бинарный поиск границы: возвращает индекс первого элемента отсортированного
 * по возрастанию списка, который больше или равен target. Если такого
 * элемента нет, возвращает values.size.
 * Пример: values = [1, 3, 3, 5, 7], target = 4 → 3 (индекс числа 5).
 */
fun lowerBound(values: List<Int>, target: Int): Int {
    TODO("Урок 15: сужайте диапазон [lo, hi) пополам, а не идите линейным проходом")
}

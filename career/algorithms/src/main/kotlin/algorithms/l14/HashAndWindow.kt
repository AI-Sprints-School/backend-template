package algorithms.l14

/**
 * Урок 14. Хеш-таблица, два указателя и скользящее окно.
 *
 * Три задачи Easy–Medium. Над каждой функцией допишите вторую строку KDoc
 * вида «Сложность: время O(?), память O(?)» — так, как код будет работать
 * после вашей реализации.
 */

/**
 * Длина самой длинной подстроки без повторяющихся символов.
 * Пример: "abcabcbb" → 3 (подстрока "abc").
 */
fun longestSubstringWithoutRepeats(s: String): Int {
    TODO("Урок 14: скользящее окно — расширяйте правую границу, сужайте левую при повторе")
}

/**
 * Индексы двух элементов списка, сумма которых равна target.
 * Гарантируется ровно одно решение; порядок индексов в результате — как
 * встретились в списке (меньший индекс первым).
 * Пример: nums = [2, 7, 11, 15], target = 9 → 0 to 1.
 */
fun twoSum(nums: List<Int>, target: Int): Pair<Int, Int>? {
    TODO("Урок 14: храните для каждого числа его индекс в хеш-таблице по ходу прохода")
}

/**
 * Группирует слова, которые являются анаграммами друг друга (одни и те же
 * буквы в любом порядке), в списки; порядок групп и слов внутри группы
 * значения не имеет — тест сравнивает множества.
 * Пример: ["eat", "tea", "tan", "ate", "nat", "bat"] →
 *   [["eat", "tea", "ate"], ["tan", "nat"], ["bat"]].
 */
fun groupAnagrams(words: List<String>): List<List<String>> {
    TODO("Урок 14: ключ группы — отсортированные буквы слова, значение в хеш-таблице — список слов")
}

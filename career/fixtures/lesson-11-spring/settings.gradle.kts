pluginManagement {
    repositories {
        maven("https://repo1.maven.org/maven2/")
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        maven("https://repo1.maven.org/maven2/")
    }
}

rootProject.name = "spring-fixture"
